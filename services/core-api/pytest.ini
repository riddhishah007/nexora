[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: hits the live local docker stack at localhost:8000 (skips if unreachable)
    llm: makes real LLM calls (slow, consumes quota) - run explicitly with -m llm
